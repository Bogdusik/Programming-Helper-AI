# 🚀 Полное руководство по деплою

## Проблема с базой данных

**Почему база данных ведет себя странно:**
1. База данных была создана **до** синхронизации схемы Prisma
2. В базе есть старые таблицы с неправильной структурой (например, колонки `email`, `name` которых больше нет в схеме)
3. Некоторые таблицы отсутствуют или имеют неправильные колонки
4. Данные могут быть в старых таблицах, но структура не соответствует схеме

**Почему не видно пользователей и сообщений:**
- Таблицы могут существовать, но с неправильной структурой
- Prisma не может правильно читать данные из-за несоответствия схемы
- Некоторые колонки отсутствуют (например, `chatSessionId` в таблице `messages`)

## ✅ Правильный порядок деплоя

### Шаг 1: Подготовка

1. **Убедитесь, что все изменения запушены в GitHub:**
   ```bash
   git add -A
   git commit -m "Final deployment preparation"
   git push origin main
   ```

2. **Проверьте переменные окружения в Vercel:**
   - `ADMIN_EMAILS` = `bogdyn13@gmail.com` (или ваш email)
   - `DATABASE_URL` - должен быть установлен
   - Все Clerk переменные должны быть установлены

### Шаг 2: Деплой на Vercel

1. **Удалите старый проект (если нужно):**
   - Vercel Dashboard → Ваш проект → Settings → Delete Project
   - Или создайте новый проект

2. **Создайте новый проект:**
   - Vercel Dashboard → Add New → Project
   - Подключите GitHub репозиторий
   - Настройте переменные окружения

3. **Дождитесь завершения деплоя**

### Шаг 3: Синхронизация базы данных (КРИТИЧНО!)

**После успешного деплоя ОБЯЗАТЕЛЬНО вызовите:**

```
POST https://your-project.vercel.app/api/final-schema-sync
```

Или через curl:
```bash
curl -X POST https://your-project.vercel.app/api/final-schema-sync
```

**Этот endpoint:**
- ✅ Удалит старые колонки (`email`, `name`, `emailVerified`, `image`)
- ✅ Добавит все недостающие колонки в таблицу `users`
- ✅ Создаст все недостающие таблицы (`messages`, `stats`, `chat_sessions`, и т.д.)
- ✅ Добавит недостающие колонки в существующие таблицы (например, `chatSessionId` в `messages`)
- ✅ Создаст все индексы и внешние ключи
- ✅ Настроит триггеры для `updatedAt`

### Шаг 4: Создание пользователя и установка админ-роли

После синхронизации схемы:

1. **Создайте пользователя в базе данных:**
   ```
   GET https://your-project.vercel.app/api/fix-admin-role
   ```
   (Вы должны быть залогинены в браузере)

2. **Проверьте, что все работает:**
   ```
   GET https://your-project.vercel.app/api/diagnose
   ```

### Шаг 5: Проверка работы

1. ✅ Откройте `/chat` - чат должен работать
2. ✅ Откройте `/stats` - статистика должна загружаться
3. ✅ Откройте `/admin` - админ-панель должна работать
4. ✅ Попробуйте отправить сообщение в чат - должно работать

## 🔧 Endpoints для диагностики и исправления

### Основные endpoints:

1. **`/api/final-schema-sync`** (POST)
   - Полная синхронизация схемы базы данных
   - **Вызовите ПЕРВЫМ после деплоя**

2. **`/api/diagnose`** (GET)
   - Диагностика всех проблем
   - Показывает состояние базы данных, переменных окружения, пользователя

3. **`/api/fix-admin-role`** (GET)
   - Создает пользователя в базе данных
   - Устанавливает админ-роль (если email в `ADMIN_EMAILS`)

4. **`/api/fix-messages-table`** (POST)
   - Исправляет структуру таблицы `messages`
   - Добавляет недостающие колонки (`chatSessionId`, `questionType`)

### Дополнительные endpoints:

- `/api/sync-all-tables` - Создает все недостающие таблицы
- `/api/sync-database` - Синхронизирует колонки в таблице `users`
- `/api/create-missing-tables` - Создает недостающие таблицы
- `/api/check-admin-data` - Проверяет данные для админ-панели

## 📋 Чеклист перед деплоем

- [ ] Все изменения запушены в GitHub
- [ ] `vercel.json` настроен правильно (без `db:push` в buildCommand)
- [ ] Все переменные окружения установлены в Vercel
- [ ] `ADMIN_EMAILS` установлен и содержит ваш email
- [ ] `DATABASE_URL` установлен и правильный

## 📋 Чеклист после деплоя

- [ ] Деплой завершен успешно
- [ ] Вызван `/api/final-schema-sync` (POST)
- [ ] Вызван `/api/fix-admin-role` (GET) - для создания пользователя
- [ ] Проверен `/api/diagnose` (GET) - все должно быть OK
- [ ] Проверена работа `/chat`, `/stats`, `/admin`

## ⚠️ Важные замечания

1. **НЕ добавляйте `db:push` в buildCommand** - это вызывает SQL ошибки на Vercel
2. **ВСЕГДА вызывайте `/api/final-schema-sync` после деплоя** - это критично!
3. **Данные из старых таблиц сохранятся** - endpoint не удаляет данные, только синхронизирует структуру
4. **Если что-то не работает** - проверьте `/api/diagnose` для диагностики

## 🐛 Решение проблем

### Проблема: "Column does not exist"
**Решение:** Вызовите `/api/final-schema-sync`

### Проблема: "Table does not exist"
**Решение:** Вызовите `/api/final-schema-sync`

### Проблема: "User not found in database"
**Решение:** Вызовите `/api/fix-admin-role`

### Проблема: "ChatSessionId does not exist"
**Решение:** Вызовите `/api/fix-messages-table` или `/api/final-schema-sync`

### Проблема: Админ-панель показывает ошибки
**Решение:** 
1. Вызовите `/api/final-schema-sync`
2. Проверьте `/api/diagnose`
3. Убедитесь, что `ADMIN_EMAILS` установлен

## 📞 Если ничего не помогает

1. Проверьте логи Vercel (Deployments → Logs)
2. Вызовите `/api/diagnose` и пришлите результат
3. Проверьте переменные окружения в Vercel
4. Убедитесь, что база данных доступна (проверьте `DATABASE_URL`)

